{
  "name": "microfrontend-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "host",
    "remote-home",
    "remote-common",
    "remote-cart",
    "remote-profile",
    "remote-orders",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:common\" \"npm run dev:home\" \"npm run dev:cart\" \"npm run dev:profile\" \"npm run dev:orders\" \"npm run dev:host\"",
    "dev:host": "npm run dev --workspace=host",
    "dev:home": "npm run dev --workspace=remote-home",
    "dev:common": "npm run dev --workspace=remote-common",
    "dev:cart": "npm run dev --workspace=remote-cart",
    "dev:profile": "npm run dev --workspace=remote-profile",
    "dev:orders": "npm run dev --workspace=remote-orders",
    "dev:backend": "npm run dev --workspace=backend",
    "build": "npm run build --workspace=backend && npm run build --workspace=remote-common && npm run build --workspace=remote-home && npm run build --workspace=remote-cart && npm run build --workspace=remote-profile && npm run build --workspace=remote-orders && npm run build --workspace=host",
    "preview": "concurrently \"npm run preview:backend\" \"npm run preview:common\" \"npm run preview:home\" \"npm run preview:cart\" \"npm run preview:profile\" \"npm run preview:orders\" \"npm run preview:host\"",
    "preview:host": "npm run preview --workspace=host",
    "preview:home": "npm run preview --workspace=remote-home",
    "preview:common": "npm run preview --workspace=remote-common",
    "preview:cart": "npm run preview --workspace=remote-cart",
    "preview:profile": "npm run preview --workspace=remote-profile",
    "preview:orders": "npm run preview --workspace=remote-orders",
    "preview:backend": "npm run start --workspace=backend",
    "start:backend": "npm run start --workspace=backend",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

